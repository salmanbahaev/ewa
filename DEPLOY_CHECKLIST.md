# ✅ Чеклист перед деплоем на VPS

## Подготовка на локальном компе

- [ ] Токен Telegram бота получен (от @BotFather)
- [ ] API ключ OpenAI получен (https://platform.openai.com/api-keys)
- [ ] Баланс OpenAI проверен (есть средства на счету)
- [ ] VPS сервер арендован (Ubuntu 20.04+)
- [ ] SSH доступ к VPS работает
- [ ] IP адрес VPS известен
- [ ] Username на VPS известен

---

## Загрузка на VPS

- [ ] Проект загружен на VPS (через SCP или Git)
- [ ] Подключение к VPS работает: `ssh user@ip`

---

## Настройка

- [ ] Создан файл `.env.local` с токенами
- [ ] Права на `.env.local` установлены: `chmod 600 .env.local`
- [ ] Скрипт деплоя имеет права: `chmod +x deploy_vps.sh`

---

## Деплой

- [ ] Запущен скрипт: `bash deploy_vps.sh`
- [ ] Скрипт завершился успешно (без ошибок)
- [ ] Статус бота активный: `sudo systemctl status ewa-bot`
- [ ] Бот отвечает в Telegram

---

## Проверка автозапуска

- [ ] Автозапуск включён: `sudo systemctl is-enabled ewa-bot`
- [ ] Перезагрузка сервера: `sudo reboot`
- [ ] После перезагрузки бот запустился автоматически
- [ ] Бот отвечает в Telegram

---

## Проверка автоперезапуска

- [ ] Остановка процесса: `sudo systemctl stop ewa-bot && sleep 15 && sudo systemctl status ewa-bot`
- [ ] Бот перезапустился автоматически
- [ ] Бот отвечает в Telegram

---

## Финальная проверка

- [ ] Логи пишутся корректно: `sudo journalctl -u ewa-bot -f`
- [ ] Нет ошибок в логах
- [ ] Бот быстро отвечает на сообщения
- [ ] История диалогов сохраняется
- [ ] Кнопка "Очистить историю" работает
- [ ] Смена ассистента (Сергей/Екатерина) работает

---

## 🎉 Готово!

Ваш бот полностью автономен и работает 24/7!

### Полезные команды

```bash
# Статус
sudo systemctl status ewa-bot

# Логи реал-тайм
sudo journalctl -u ewa-bot -f

# Перезапуск
sudo systemctl restart ewa-bot

# Остановка
sudo systemctl stop ewa-bot

# Отключить автозапуск
sudo systemctl disable ewa-bot

# Включить автозапуск
sudo systemctl enable ewa-bot
```



