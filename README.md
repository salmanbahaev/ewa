# EWA Product - AI Telegram Bot

AI-консультант для компании EWA PRODUCT. Бот помогает пользователям подбирать продукты, отвечает на вопросы о здоровье и компании.

## Возможности

✅ Консультации по здоровью и витаминам  
✅ Умный поиск продуктов в каталоге  
✅ Информация о компании и партнерстве  
✅ Расписание событий и адреса офисов  
✅ История диалогов с возможностью очистки  

## Технологии

- **Python 3.10+**
- **aiogram 3.x** - Telegram Bot Framework
- **OpenAI GPT-4o-mini** - AI с Function Calling
- **SQLite** - хранение истории диалогов
- **Loguru** - логирование

## Установка

### 1. Клонирование и установка зависимостей

```bash
# Установка Python зависимостей
pip install -r requirements.txt
```

### 2. Настройка окружения

Создайте файл `.env` на основе `.env.sample`:

```bash
cp .env.sample .env
```

Отредактируйте `.env` и укажите:

```env
# Telegram Bot Token (получить из @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# OpenAI API Key
OPENAI_API_KEY=your_openai_key_here

# Модель (по умолчанию gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
```

### 3. Запуск бота

```bash
python main.py
```

## Структура проекта

```
ewa/
├── bot/               # Telegram bot
│   ├── handlers/      # Обработчики команд
│   ├── keyboards/     # Клавиатуры
│   └── middlewares/   # Middleware
├── ai/                # AI логика
│   ├── assistant.py   # OpenAI интеграция
│   ├── prompts.py     # System prompts
│   └── product_search.py  # Поиск продуктов
├── data/              # Данные
│   ├── database.py    # SQLite
│   ├── catalog.json   # Каталог товаров
│   ├── company.json   # О компании
│   ├── business.json  # Партнерство
│   ├── events.json    # События
│   └── geography.json # Адреса
├── logs/              # Логи (создается автоматически)
├── config.py          # Конфигурация
└── main.py            # Точка входа
```

## Деплой на VPS (Ubuntu)

### 1. Установка Python и зависимостей

```bash
sudo apt update
sudo apt install python3.10 python3-pip git -y
```

### 2. Загрузка проекта

```bash
git clone <your_repo>
cd ewa
pip3 install -r requirements.txt
```

### 3. Настройка .env

```bash
nano .env
# Вставьте ваши токены
```

### 4. Создание systemd service

```bash
sudo nano /etc/systemd/system/ewa-bot.service
```

Вставьте содержимое из `ewa-bot.service.example`

### 5. Запуск сервиса

```bash
sudo systemctl daemon-reload
sudo systemctl enable ewa-bot
sudo systemctl start ewa-bot

# Проверка статуса
sudo systemctl status ewa-bot

# Просмотр логов
sudo journalctl -u ewa-bot -f
```

## Команды бота

- `/start` - Начать диалог
- Любой текст - Задать вопрос ассистенту
- 🗑 **Очистить историю** (кнопка) - Удалить историю диалога

## Примеры вопросов

**Общие вопросы:**
- "Как витамин D влияет на организм?"
- "Что важно для здоровья кожи?"

**Поиск продуктов:**
- "Что порекомендуете для улучшения памяти?"
- "Какие товары для витамина С?"
- "Что-то для хорошего сна"

**О компании:**
- "Расскажите о компании EWA"
- "Где находится офис в Москве?"
- "Как стать партнером?"

## Тестирование

```bash
# Запуск тестов
pytest tests/ -v
```

## Логи

Логи сохраняются в папке `logs/`:
- Консоль: цветной вывод для разработки
- Файлы: `bot_YYYY-MM-DD.log` (ротация ежедневно, хранение 7 дней)

## Troubleshooting

**Бот не запускается:**
- Проверьте `.env` файл
- Убедитесь что токены корректны
- Проверьте логи: `tail -f logs/bot_*.log`

**Ошибки OpenAI:**
- Проверьте баланс на аккаунте OpenAI
- Убедитесь что API ключ активен

**База данных:**
- База создается автоматически в `data/bot_database.db`
- Для сброса: удалите файл БД и перезапустите бота

## Контакты

Проект разработан для компании EWA PRODUCT  
Сайт: https://ewaproduct.com/

