# 📦 Файлы для деплоя - Справка

## 🎯 Для быстрого деплоя используй:

### **DEPLOY_QUICK.md** ⚡
Краткая шпаргалка - 3 простых шага за 5 минут

---

## 📚 Все файлы деплоя

| Файл | Назначение |
|------|-----------|
| **DEPLOY_QUICK.md** | ⚡ Быстрая шпаргалка (3 шага) |
| **DEPLOY.md** | 📖 Полная инструкция с деталями |
| **deploy_vps.sh** | 🤖 Скрипт автоматического деплоя |
| **DEPLOY_CHECKLIST.md** | ✅ Чеклист проверки после деплоя |
| **ewa-bot.service.example** | ⚙️ Шаблон systemd service |
| **.gitattributes** | 🔧 Настройка переводов строк |

---

## 🚀 Порядок действий

### 1. Подготовка
Прочитай **DEPLOY_CHECKLIST.md** - убедись что всё готово

### 2. Деплой
Следуй **DEPLOY_QUICK.md** для быстрого деплоя  
Или **DEPLOY.md** для детальной инструкции

### 3. Проверка
Пройдись по **DEPLOY_CHECKLIST.md** - проверь всё работает

---

## ⚙️ Что настраивается автоматически

Скрипт **deploy_vps.sh** автоматически:

✅ Устанавливает Python (если нужно)  
✅ Создаёт виртуальное окружение  
✅ Устанавливает все зависимости  
✅ Создаёт systemd service с правильными путями  
✅ Включает автозапуск при загрузке сервера  
✅ Настраивает автоперезапуск при падении  
✅ Устанавливает лимиты ресурсов (RAM, CPU)  
✅ Запускает бота  

---

## 🛡️ Надёжность

### Автозапуск при загрузке сервера
- `WantedBy=multi-user.target` - запуск с системой
- `After=network-online.target` - после появления сети

### Автоперезапуск при падении
- `Restart=always` - всегда перезапускать
- `RestartSec=10` - задержка 10 секунд
- `StartLimitInterval=300` - за 5 минут
- `StartLimitBurst=5` - максимум 5 перезапусков

### Лимиты ресурсов
- `MemoryLimit=512M` - лимит памяти
- `CPUQuota=50%` - лимит CPU

---

## 🔧 Управление

После деплоя используй:

```bash
# Статус
sudo systemctl status ewa-bot

# Логи
sudo journalctl -u ewa-bot -f

# Перезапуск
sudo systemctl restart ewa-bot
```

Полный список команд в конце **DEPLOY_QUICK.md**

---

## 📞 Нужна помощь?

1. Проверь **DEPLOY_CHECKLIST.md** - может что-то пропущено
2. Смотри логи: `sudo journalctl -u ewa-bot -n 100`
3. Проверь `.env.local`: `cat .env.local`
4. Проверь статус: `sudo systemctl status ewa-bot`



