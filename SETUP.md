# 🚀 Быстрый старт

## Шаг 1: Получите токен Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: `EWA Product Assistant`)
   - Введите username бота (например: `ewa_product_bot`)
4. Скопируйте полученный токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Настройте конфигурацию

Откройте файл **`.env.local`** и заполните:

```env
# Telegram Bot Token (вставьте токен из BotFather)
TELEGRAM_BOT_TOKEN=СЮДА_ВАШ_ТОКЕН

# OpenAI API Key
OPENAI_API_KEY=ваш_openai_api_ключ

# Модель (оставьте как есть)
OPENAI_MODEL=gpt-4o-mini
```

## Шаг 3: Запустите бота

```bash
python main.py
```

## Проверка работы

1. Найдите вашего бота в Telegram по username
2. Нажмите **Start** или отправьте `/start`
3. Задайте вопрос, например:
   - "Что порекомендуете для памяти?"
   - "Расскажите о компании"
   - "Где офис в Москве?"

## Остановка бота

Нажмите **Ctrl+C** в терминале

---

## Возможные проблемы

### ❌ `ValueError: TELEGRAM_BOT_TOKEN is not set`
**Решение:** Проверьте что в `.env.local` заполнен `TELEGRAM_BOT_TOKEN`

### ❌ `openai.AuthenticationError`
**Решение:** Проверьте правильность OpenAI API ключа

### ❌ Бот не отвечает
**Решение:** 
- Проверьте что бот запущен (должна быть надпись "Starting polling...")
- Проверьте логи в консоли на ошибки
- Убедитесь что токен бота правильный

---

## Логи

Все логи сохраняются в папке `logs/`:
- `bot_2025-11-06.log` - файл с логами за день

Логи помогут понять что происходит при ошибках.

